# Leave VITE_AG0_URL empty for same-origin: when the UI is embedded in the ag0
# binary, it resolves to window.location.origin at runtime. Override only when
# running `vite dev` against a separately-hosted ag0 (e.g. http://127.0.0.1:9091).
VITE_AG0_URL=
VITE_SESSION_ID=default-session-1